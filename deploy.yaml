deployments:
  - id: app
    label: app.budgie.help
    cwd: demo
    command:
      - /bin/bash
      - -lc
      - npm ci && npm run build && ./deploy.sh
    app: budgie
    purgeDomains:
      - app.budgie.help
